<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/same_color"
    android:fitsSystemWindows="true"
    tools:context="trackmyspend.budgetplanner.expensemanager.OnBoarding_Screen.Name_Input_Fragment">

    <LinearLayout
        android:id="@+id/rootNameInput"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:padding="24dp"
        tools:context="trackmyspend.budgetplanner.expensemanager.OnBoarding_Screen.Name_Input_Fragment">

        <!-- ========================= -->
        <!-- Onboarding Header (Welcome) -->
        <!-- ========================= -->
        <LinearLayout
            android:id="@+id/headerOnboarding"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:layout_marginTop="10dp"
            android:visibility="visible">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Welcome to"
                android:textColor="@color/opposite_color"
                android:textSize="16sp"
                android:alpha="0.9" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="-6dp"
                android:text="TrackMySpend"
                android:textStyle="bold"
                android:textColor="@color/nav_icon_active"
                android:textSize="36sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:layout_marginTop="5dp"
                android:textColor="@color/gray"
                android:text="Smart way to track your expenses, manage budgets, and control your money."
                android:textSize="10dp"/>
        </LinearLayout>

        <!-- ========================= -->
        <!-- Profile Header -->
        <!-- ========================= -->
        <LinearLayout
            android:id="@+id/headerProfile"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="20dp"
            android:visibility="gone">

            <!-- Back Button -->
            <ImageView
                android:id="@+id/ivBack"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/ic_back_btn"
                android:tint="@color/opposite_color" />

            <!-- Title -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:text="Edit Name"
                android:textSize="18sp"
                android:textColor="@color/opposite_color"
                android:textStyle="bold" />

            <!-- Close Button (optional, invisible if not used) -->
            <ImageView
                android:id="@+id/ivClose"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:visibility="gone" />
        </LinearLayout>

        <!-- ========================= -->
        <!-- Name Input -->
        <!-- ========================= -->
        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/layoutName"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter your name"
            android:layout_marginTop="20dp">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="textPersonName"
                android:imeOptions="actionDone" />
        </com.google.android.material.textfield.TextInputLayout>

        <TextView
            android:id="@+id/btnSaveName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:background="@drawable/bg_button_currency_select"
            android:gravity="center"
            android:paddingVertical="12dp"
            android:text="Next"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:clickable="true"
            android:focusable="true" />

    </LinearLayout>

</RelativeLayout>