<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <!-- Normal state with solid white border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#7E000000" /> <!-- Semi-transparent background -->
            <stroke
                android:width="0.5dp"
            android:color="#61FFFFFF" />
            <corners android:radius="12dp" />      <!-- Match with ripple mask -->
        </shape>
    </item>

    <!-- Ripple Effect -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />   <!-- White ripple -->
            <corners android:radius="16dp" />
        </shape>
    </item>
</ripple>
