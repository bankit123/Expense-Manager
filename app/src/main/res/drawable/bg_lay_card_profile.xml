<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/main_bg" />

    <!-- Rounded corners -->
    <corners android:radius="8dp" />

    <!-- Thin border -->
    <stroke
        android:width="1dp"
        android:color="@color/setting_boarder" /> <!-- Border color -->
</shape>
